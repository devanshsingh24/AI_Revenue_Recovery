"""Placeholder kept for backwards-compatible imports.

Execution lives in ``src.razorpay_client.RazorpayClient.execute_recovery_action``
(invoked by ``src.agent.nodes.execute_node``). This module intentionally defines
no behavior; it exists only so legacy ``import src.execution`` statements do not
break.
"""
